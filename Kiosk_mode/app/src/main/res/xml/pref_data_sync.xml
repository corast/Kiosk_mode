<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <CheckBoxPreference
        android:defaultValue="true"
        android:title="@string/settings_data_sync_automatic_title"
        android:key="@string/KEY_DATA_SYNCHRONIZE_AUTOMATIC"
        android:summaryOn="@string/settings_data_sync_automatic_summary_on"
        android:summaryOff="@string/settings_data_sync_automatic_summary_off"
        />


    <!--
    <SwitchPreference
        android:defaultValue="true"
        android:key="@string/KEY_DATA_SYNCH_AUTOMATIC"
        android:title="@string/settings_data_sync_automatic_title"
        android:summary="@string/settings_data_sync_automatic_summary">
    </SwitchPreference>
    -->

    <!-- Settings som vi kanksje bare fjerner.
    <Preference android:title="Geofence Update Automaticly">
        <intent android:action="android.settings.SYNC_GEOFENCE_SETTINGS"/>
    </Preference> -->

    <EditTextPreference
        android:defaultValue="@string/DEFAULT_URL_SERVER_STATISTICS"
        android:inputType="textUri"
        android:key="@string/KEY_URL_SERVER_STATISTICS"
        android:selectAllOnFocus="false"
        android:title="@string/settings_data_sync_url_statistics_title"
        />

    <EditTextPreference
        android:defaultValue="@string/DEFAULT_URL_SERVER_GEOFENCE"
        android:inputType="textUri"
        android:key="@string/KEY_URL_SERVER_GEOFENCE"
        android:selectAllOnFocus="false"
        android:title="@string/settings_data_sync_url_geofence_title"
        />

</PreferenceScreen>
